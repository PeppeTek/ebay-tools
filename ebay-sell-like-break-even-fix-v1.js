javascript:(()=>{
'use strict';
const PANEL_ID='capitan-sell-like-clone';
const ENDPOINT_KEY='pep-ebay-bs-v6-google-url';
const PATCH_ID='capitan-break-even-fix-v3';
if(document.getElementById(PATCH_ID))return;
const marker=document.createElement('span');marker.id=PATCH_ID;marker.style.display='none';document.documentElement.appendChild(marker);
const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const visible=el=>!!(el&&el.getClientRects&&el.getClientRects().length);
let rates={ebayFee:.136,internationalFee:.016,marketingFee:.02,vatOnFees:.22,salesTaxEstimate:.06,fixedFee:.40};
function endpoint(){return (localStorage.getItem(ENDPOINT_KEY)||'').replace(/\/+$/,'')}
function jsonpAction(action){const ep=endpoint();return new Promise((resolve,reject)=>{if(!ep)return reject(Error('Endpoint Apps Script non configurato'));const cb='__capitanBeCb_'+Date.now()+'_'+Math.floor(Math.random()*1e6),s=document.createElement('script'),t=setTimeout(()=>done(Error('Timeout backend')),30000);function done(err,val){clearTimeout(t);try{delete window[cb]}catch(_){window[cb]=undefined}s.remove();err?reject(err):resolve(val)}window[cb]=v=>done(null,v);s.onerror=()=>done(Error('Backend non raggiungibile'));const q=new URLSearchParams({action,callback:cb,_:Date.now().toString()});s.src=ep+(ep.includes('?')?'&':'?')+q.toString();document.head.appendChild(s)})}
function salePrice(){const p=document.getElementById(PANEL_ID);if(!p)return null;const rows=[...p.querySelectorAll('#steps .row')];const row=rows.find(r=>/^Prezzo(?: di vendita \(-2%\))?:/i.test(clean(r.innerText||r.textContent)));const t=clean(row?.innerText||row?.textContent||'');const m=t.match(/Prezzo(?: di vendita \(-2%\))?:\s*([0-9]+(?:[.,][0-9]+)?)/i);return m?Number(m[1].replace(',','.')):null}
function calc(s,r){s=Number(s);if(!isFinite(s)||s<=0)return null;const feeRate=(r.ebayFee||0)+(r.internationalFee||0)+(r.marketingFee||0);const variable=(1+(r.salesTaxEstimate||0))*feeRate*(1+(r.vatOnFees||0));const fixed=(r.fixedFee||0)*(1+(r.vatOnFees||0));const v=(s*(1-variable))-fixed;return v>0?Math.round(v*100)/100:null}
function ensureRow(){const p=document.getElementById(PANEL_ID);const steps=p?.querySelector('#steps');if(!steps)return null;let row=p.querySelector('#capitan-break-even-row');if(!row){const pr=[...steps.querySelectorAll('.row')].find(r=>/^Prezzo(?: di vendita \(-2%\))?:/i.test(clean(r.innerText||r.textContent)));row=document.createElement('div');row.id='capitan-break-even-row';row.className='row';row.innerHTML='<b>Break Even Price:</b> <span id="capitan-break-even-value">—</span>';if(pr)pr.insertAdjacentElement('afterend',row);else steps.prepend(row)}const old=row.querySelector('#capitan-pricing-open');if(old)old.remove();return row.querySelector('#capitan-break-even-value')}
function refresh(){const el=ensureRow();if(!el)return false;const sp=salePrice();const v=calc(sp,rates);el.textContent=v==null?'—':`${v.toFixed(2)} USD`;const danger=v!=null&&isFinite(sp)&&v>sp;el.style.color=danger?'#b42318':'';el.style.fontWeight=danger?'700':'';window.dispatchEvent(new CustomEvent('capitan-break-even-updated',{detail:{salePrice:sp,breakEven:v,rates}}));return v!=null}
async function load(){refresh();try{const d=await jsonpAction('sell_like_pricing_get');if(d&&d.ok&&d.rates)rates=d.rates}catch(e){console.warn('Break Even pricing',e)}refresh()}
window.addEventListener('capitan-pricing-saved',e=>{if(e.detail?.rates)rates=e.detail.rates;refresh()});
let tries=0;const timer=setInterval(()=>{tries++;if(refresh()||tries>200)clearInterval(timer)},100);
load();

function selectedAsins(){const p=document.getElementById(PANEL_ID);return p?[...p.querySelectorAll('input.capitan-amazon-choice:checked')].map(x=>clean(x.value)).filter(Boolean):[]}
function directSkuFields(){const selectors=['input[name*="sku" i]','textarea[name*="sku" i]','input[id*="sku" i]','textarea[id*="sku" i]','input[aria-label*="custom label" i]','textarea[aria-label*="custom label" i]','input[placeholder*="custom label" i]','textarea[placeholder*="custom label" i]','input[data-testid*="sku" i]','textarea[data-testid*="sku" i]'];const out=[];for(const s of selectors){try{document.querySelectorAll(s).forEach(x=>out.push(x))}catch(_){}}return [...new Set(out)]}
function findSkuField(){const direct=directSkuFields();const vis=direct.find(visible);if(vis)return vis;const re=/custom\s*label(?:\s*\(\s*sku\s*\))?|seller\s*sku|merchant\s*sku|^sku$/i;for(const l of document.querySelectorAll('label')){if(!re.test(clean(l.innerText||l.textContent||'')))continue;let el=l.htmlFor?document.getElementById(l.htmlFor):l.querySelector('input,textarea');if(el)return el;let p=l.parentElement;for(let i=0;i<7&&p;i++,p=p.parentElement){el=p.querySelector('input,textarea');if(el)return el}}for(const n of document.querySelectorAll('span,div,p,strong')){const t=clean(n.innerText||n.textContent||'');if(t.length>70||!re.test(t))continue;let p=n.parentElement;for(let i=0;i<7&&p;i++,p=p.parentElement){const els=[...p.querySelectorAll('input,textarea')];const el=els.find(x=>/sku|custom.?label/i.test(clean([x.name,x.id,x.getAttribute('aria-label'),x.placeholder].join(' '))))||((els.length===1)?els[0]:null);if(el)return el}}return direct[0]||null}
async function revealSku(){const re=/custom\s*label|seller\s*sku|merchant\s*sku|\bsku\b/i;const targets=[...document.querySelectorAll('button,[role="button"],summary,a')].filter(visible).filter(x=>re.test(clean((x.innerText||x.textContent||'')+' '+(x.getAttribute('aria-label')||''))));for(const b of targets){try{b.click();await sleep(180);const f=findSkuField();if(f)return f}catch(_){}}const more=[...document.querySelectorAll('button,[role="button"],summary,a')].filter(visible).find(x=>/show more|more options|optional|additional/i.test(clean((x.innerText||x.textContent||'')+' '+(x.getAttribute('aria-label')||''))));if(more){try{more.click();await sleep(250)}catch(_){}}return findSkuField()}
function setNative(el,value){const proto=el instanceof HTMLTextAreaElement?HTMLTextAreaElement.prototype:HTMLInputElement.prototype;const setter=Object.getOwnPropertyDescriptor(proto,'value')?.set;const old=el.value;el.focus();if(setter)setter.call(el,value);else el.value=value;if(el._valueTracker&&typeof el._valueTracker.setValue==='function')el._valueTracker.setValue(old);try{el.dispatchEvent(new InputEvent('input',{bubbles:true,inputType:'insertText',data:value}))}catch(_){el.dispatchEvent(new Event('input',{bubbles:true}))}el.dispatchEvent(new Event('change',{bubbles:true}));el.dispatchEvent(new Event('blur',{bubbles:true}));el.blur()}
async function writeSku(value){let field=findSkuField();if(!field)field=await revealSku();if(!field)return {ok:false,reason:'Campo Custom label (SKU) non trovato'};for(let i=0;i<3;i++){setNative(field,value);await sleep(220);if(clean(field.value)===clean(value))return {ok:true,field}}return {ok:false,reason:'eBay non ha mantenuto il valore SKU'}}
function setStatus(html){const p=document.getElementById(PANEL_ID),s=p?.querySelector('#capitan-amazon-status');if(s)s.innerHTML=html}
document.addEventListener('click',async e=>{const btn=e.target.closest('#capitan-amazon-insert');if(!btn)return;e.preventDefault();e.stopImmediatePropagation();const asins=selectedAsins();if(!asins.length){setStatus('<span style="color:#b42318;font-weight:700">Seleziona almeno un ASIN.</span>');return}const value=asins.join(' - ');btn.disabled=true;setStatus('<span style="color:#555">Inserimento ASIN nel Custom label (SKU)…</span>');try{const r=await writeSku(value);if(!r.ok)throw Error(r.reason);setStatus('<span style="color:#137333;font-weight:700">ASIN inseriti nel Custom label (SKU):</span> '+value)}catch(err){setStatus('<span style="color:#b42318;font-weight:700">'+String(err.message||err)+'</span>')}finally{btn.disabled=false}},true);
})();